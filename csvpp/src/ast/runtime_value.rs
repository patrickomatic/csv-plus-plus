//! # RuntimeValue
//!
// use serde::{Deserialize, Serialize};
// use std::str;
/*
use std::fmt;

// use crate::Error;

// XXX maybe get rid of this entirely and it can just be a variable?
// #[derive(Debug, Deserialize, PartialEq, Serialize)]
#[derive(Debug, PartialEq)]
pub struct RuntimeValue(String);

impl super::Node for RuntimeValue {}

impl fmt::Display for RuntimeValue {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        write!(f, "(runtime value)")
    }
}

impl str::FromStr for RuntimeValue {
    type Err = Error;

    fn from_str(input: &str) -> Result<Self, Self::Err> {
        todo!();
        // Ok(RuntimeValue(input.to_string()))
    }
}

#[cfg(test)]
mod tests {
    // use super::*;
}
*/
