fees := 0.65 # my broker charges $0.65 a trade

is_call := EQ($$rownum, "call")
commission := IFS($$is_call, ADD($$rownum * $$fees, $$rownum))

profit := MULTIPLY(INDIRECT("C" & $$rownum), INDIRECT("D" & $$rownum))
---
![[format=bold/align=center/freeze]]Date,Purchase,Price,Quantity,Total,Fees
![[expand]],[[format=bold]],,,"$$profit",
